DYN_STRATEGY="NO_BACKOFF|SAMPLING|FIXED_BACKOFF|EMPTY|COUNT_ONLY" (Backoff strategy)
DYN_SAMPLE_SIZE=<long> (Sample size per epoch)
DYN_SAMPLE_PERIOD=<double> (Sampling epoch in seconds)
DYN_OUTPUT_TYPE="PRETTY|CSV|BOTH|STRIPPED" (Dynaprof output. BOTH means both PRETTY and CSV files would be generated)
DYN_FILTER_PROBES=<comma_seperated_list> (List of functions to be profiled. Rest of the functions are ignored)
DYN_FILTER_FILE=<file> (File with list of functions to be profiled. Each function name should be in a newline)


